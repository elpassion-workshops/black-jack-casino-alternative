p Current score: #{@player_score.to_s} Dealer score: #{@dealer_score.to_s}
p = link_to 'Hit', black_jack_hit_path
p = link_to 'Stand', black_jack_stand_path

p Player cards: 
- @player_cards.each do |front, back|
  p = front
  
p Dealer cards: 
- @dealer_cards.each do |front, back|
  p = front
  
p = "#{@winner[0]} is the winner, score: #{@winner[1]}" if @winner
